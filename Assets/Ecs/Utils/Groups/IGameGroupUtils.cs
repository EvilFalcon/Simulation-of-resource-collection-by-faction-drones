using System;
using System.Collections.Generic;

namespace Ecs.Utils.Groups
{
    public interface IGameGroupUtils
    {
        // IDisposable GetLines(out List<GameEntity> buffer, Func<GameEntity, bool> filter = null);
        // IDisposable GetLinesWithActiveCheck(out List<GameEntity> buffer, bool isActive, Func<GameEntity, bool> filter = null);
        // IDisposable GetMovableFigures(out List<GameEntity> buffer, Func<GameEntity, bool> filter = null);
        // IDisposable GetMovableFigureWithActiveCheck(out List<GameEntity> buffer, bool isActive, Func<GameEntity, bool> filter = null);
    }
}
